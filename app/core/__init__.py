"""Core cross-cutting concerns: configuration, logging, and error handling.

These modules intentionally depend on nothing in the API, service, or
persistence layers, so they can be imported anywhere without creating cycles.
"""
